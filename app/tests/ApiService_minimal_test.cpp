#include <gtest/gtest.h>

// Minimal test to check if ApiService tests are being discovered
TEST(ApiServiceMinimalTest, BasicTest) {
    EXPECT_TRUE(true);
}

TEST(ApiServiceMinimalTest, AnotherTest) {
    EXPECT_EQ(1, 1);
}
